/target/
**/*.rs.bk
cmake-build-debug
CMakeLists.txt
auth_token.txt
.rustfmt.toml
build.log
build_*.log
# Release bundles, whatever they are named
AlfredPinboardRust*.alfredworkflow
Cargo.toml.back
run_exec.sh
# The built binary, staged here by a local run of create_alfred_workflow.sh.
# Anchored so it cannot accidentally match a directory elsewhere in the tree.
/res/workflow/alfred-pinboard-rs
.DS_Store
